# Private source data

The download script saves the SCMS extract here using the public mirror documented in the data contract; the historical official endpoint was unavailable during retrieval. This directory is ignored by Git because the source license metadata requires separate verification before redistribution.

Run `python scripts/download_scms.py` to fetch the source data locally.
